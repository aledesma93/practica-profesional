import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appEdificio.app',
  appName: 'appEdificio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.usersAdmin.app',
  appName: 'usersAdmin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
